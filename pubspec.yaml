name: contas
description: "Gerenciador Financeiro Offline"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.2
  intl: ^0.19.0
  share_plus: ^10.0.0
  file_picker: ^8.0.0
  file_saver: ^0.2.0
  local_auth: ^2.2.0
  pdf: ^3.11.3
  printing: ^5.14.2
  http: ^1.6.0
  url_launcher: ^6.3.2
  shared_preferences: ^2.5.4

flutter:
  uses-material-design: true